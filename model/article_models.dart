class ArticleModels {
  String author, title, description, url, urlToImage, content;
  ArticleModels(
      {this.author,
      this.content,
      this.description,
      this.title,
      this.url,
      this.urlToImage});
}
